# Notes:
This is my first project in Node.

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Base for multiplication table              [número] [requerido]
  -l, --list     Flag to show the table when it is done
                                                     [booleano] [defecto: false]
  -h, --limit    Limit for multiplication table           [número] [defecto: 10]

```